class Solution(object):
    def reverseString(self, s):
        
        s = ["h","e","l","l","o"]
        left = s[i]
        right = s[i] - 1

        while (left < right)
        left, right = right, left
        else stop

        return s;
        